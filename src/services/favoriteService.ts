class FavoriteService {
    getFavorites(userId: string) { }
    addFavorite(userId: string, veiculoId: string) { }
    removeFavorite(userId: string, veiculoId: string) { }
    isFavorite(userId: string, veiculoId: string) { }
}

export default new FavoriteService();